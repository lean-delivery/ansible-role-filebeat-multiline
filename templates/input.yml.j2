- {{ filebeat_inputs_type }}: log
  enabled: True
  paths: 
    {{ fb_input.paths | to_nice_yaml | indent(4, false) }}
  fields: 
    {{ fb_input.fields | to_nice_yaml | indent(4, false) }}
  fields_under_root: True
  close_inactive: 2m